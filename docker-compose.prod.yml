services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://api.evready.pk}
    image: evready-frontend:prod
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:80"
